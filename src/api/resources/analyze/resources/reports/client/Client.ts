// This file was auto-generated by Fern from our API Definition.

import type { BaseClientOptions, BaseRequestOptions } from "../../../../../../BaseClient";
import { type NormalizedClientOptionsWithAuth, normalizeClientOptionsWithAuth } from "../../../../../../BaseClient";
import * as core from "../../../../../../core";
import { mergeHeaders } from "../../../../../../core/headers";
import * as environments from "../../../../../../environments";
import { handleNonStatusCodeError } from "../../../../../../errors/handleNonStatusCodeError";
import * as errors from "../../../../../../errors/index";
import * as serializers from "../../../../../../serialization/index";
import * as Webflow from "../../../../../index";

export declare namespace ReportsClient {
    export type Options = BaseClientOptions;

    export interface RequestOptions extends BaseRequestOptions {}
}

export class ReportsClient {
    protected readonly _options: NormalizedClientOptionsWithAuth<ReportsClient.Options>;

    constructor(options: ReportsClient.Options) {
        this._options = normalizeClientOptionsWithAuth(options);
    }

    /**
     * Returns a daily time series of a single metric — sessions, users, or pageviews — over a time window.
     *
     * Filter the report with top-level query parameters (`country`, `deviceType`, `pagePath`, etc.) or via the `filter` parameter for multi-value and negation matching.
     *
     * <Warning title="Analyze add-on required">This endpoint requires a workspace with the Analyze add-on.</Warning>
     *
     * <Note title="Concurrency limit: 1 request at a time">Each access token can have one Analyze request in flight at a time, across all Analyze endpoints. Additional concurrent requests return `429 Too Many Requests`; wait for your in-flight request to finish, or for the `Retry-After` interval, then retry.</Note>
     *
     * Required scope | `sites:read`
     *
     * @param {string} site_id - Unique identifier for a Site
     * @param {Webflow.analyze.ReportsTrafficRequest} request
     * @param {ReportsClient.RequestOptions} requestOptions - Request-specific configuration.
     *
     * @throws {@link Webflow.BadRequestError}
     * @throws {@link Webflow.UnauthorizedError}
     * @throws {@link Webflow.ForbiddenError}
     * @throws {@link Webflow.NotFoundError}
     * @throws {@link Webflow.TooManyRequestsError}
     * @throws {@link Webflow.InternalServerError}
     *
     * @example
     *     await client.analyze.reports.traffic("580e63e98c9a982ac9b8b741", {
     *         startTime: new Date("2026-04-01T00:00:00.000Z"),
     *         endTime: new Date("2026-04-08T00:00:00.000Z"),
     *         metricScope: "session",
     *         bucketTimeZone: "America/New_York",
     *         deviceType: "desktop",
     *         country: "US",
     *         pagePath: "/towels",
     *         trafficSource: "SO",
     *         referrer: "google.com",
     *         browser: "Chrome",
     *         utmCampaign: "dont-panic-2026",
     *         utmMedium: "email",
     *         utmSource: "hitchhikers-guide"
     *     })
     */
    public traffic(
        site_id: string,
        request: Webflow.analyze.ReportsTrafficRequest,
        requestOptions?: ReportsClient.RequestOptions,
    ): core.HttpResponsePromise<Webflow.TrafficResponse> {
        return core.HttpResponsePromise.fromPromise(this.__traffic(site_id, request, requestOptions));
    }

    private async __traffic(
        site_id: string,
        request: Webflow.analyze.ReportsTrafficRequest,
        requestOptions?: ReportsClient.RequestOptions,
    ): Promise<core.WithRawResponse<Webflow.TrafficResponse>> {
        const {
            startTime,
            endTime,
            metricScope,
            bucketTimeZone,
            deviceType,
            country,
            pagePath,
            trafficSource,
            referrer,
            browser,
            utmCampaign,
            utmMedium,
            utmSource,
            filter,
        } = request;
        const _queryParams: Record<string, unknown> = {
            startTime: startTime.toISOString(),
            endTime: endTime.toISOString(),
            metricScope: serializers.TrafficMetricScope.jsonOrThrow(metricScope, {
                unrecognizedObjectKeys: "passthrough",
                allowUnrecognizedUnionMembers: true,
                allowUnrecognizedEnumValues: true,
                omitUndefined: true,
            }),
            bucketTimeZone,
            deviceType:
                deviceType != null
                    ? serializers.analyze.ReportsTrafficRequestDeviceType.jsonOrThrow(deviceType, {
                          unrecognizedObjectKeys: "passthrough",
                          allowUnrecognizedUnionMembers: true,
                          allowUnrecognizedEnumValues: true,
                          omitUndefined: true,
                      })
                    : undefined,
            country,
            pagePath,
            trafficSource,
            referrer,
            browser,
            utmCampaign,
            utmMedium,
            utmSource,
            filter:
                filter != null
                    ? serializers.TrafficFilter.jsonOrThrow(filter, {
                          unrecognizedObjectKeys: "passthrough",
                          allowUnrecognizedUnionMembers: true,
                          allowUnrecognizedEnumValues: true,
                          omitUndefined: true,
                          breadcrumbsPrefix: ["request", "filter"],
                      })
                    : filter,
        };
        const _authRequest: core.AuthRequest = await this._options.authProvider.getAuthRequest();
        const _headers: core.Fetcher.Args["headers"] = mergeHeaders(
            _authRequest.headers,
            this._options?.headers,
            requestOptions?.headers,
        );
        const _response = await core.fetcher({
            url: core.url.join(
                (await core.Supplier.get(this._options.baseUrl)) ??
                    ((await core.Supplier.get(this._options.environment)) ?? environments.WebflowEnvironment.DataApi)
                        .base,
                `sites/${core.url.encodePathParam(site_id)}/analyze/reports/traffic`,
            ),
            method: "GET",
            headers: _headers,
            queryParameters: { ..._queryParams, ...requestOptions?.queryParams },
            timeoutMs: (requestOptions?.timeoutInSeconds ?? this._options?.timeoutInSeconds ?? 60) * 1000,
            maxRetries: requestOptions?.maxRetries ?? this._options?.maxRetries,
            abortSignal: requestOptions?.abortSignal,
            fetchFn: this._options?.fetch,
            logging: this._options.logging,
        });
        if (_response.ok) {
            return {
                data: serializers.TrafficResponse.parseOrThrow(_response.body, {
                    unrecognizedObjectKeys: "passthrough",
                    allowUnrecognizedUnionMembers: true,
                    allowUnrecognizedEnumValues: true,
                    skipValidation: true,
                    breadcrumbsPrefix: ["response"],
                }),
                rawResponse: _response.rawResponse,
            };
        }

        if (_response.error.reason === "status-code") {
            switch (_response.error.statusCode) {
                case 400:
                    throw new Webflow.BadRequestError(_response.error.body, _response.rawResponse);
                case 401:
                    throw new Webflow.UnauthorizedError(
                        serializers.Error_.parseOrThrow(_response.error.body, {
                            unrecognizedObjectKeys: "passthrough",
                            allowUnrecognizedUnionMembers: true,
                            allowUnrecognizedEnumValues: true,
                            skipValidation: true,
                            breadcrumbsPrefix: ["response"],
                        }),
                        _response.rawResponse,
                    );
                case 403:
                    throw new Webflow.ForbiddenError(_response.error.body, _response.rawResponse);
                case 404:
                    throw new Webflow.NotFoundError(
                        serializers.Error_.parseOrThrow(_response.error.body, {
                            unrecognizedObjectKeys: "passthrough",
                            allowUnrecognizedUnionMembers: true,
                            allowUnrecognizedEnumValues: true,
                            skipValidation: true,
                            breadcrumbsPrefix: ["response"],
                        }),
                        _response.rawResponse,
                    );
                case 429:
                    throw new Webflow.TooManyRequestsError(
                        serializers.Error_.parseOrThrow(_response.error.body, {
                            unrecognizedObjectKeys: "passthrough",
                            allowUnrecognizedUnionMembers: true,
                            allowUnrecognizedEnumValues: true,
                            skipValidation: true,
                            breadcrumbsPrefix: ["response"],
                        }),
                        _response.rawResponse,
                    );
                case 500:
                    throw new Webflow.InternalServerError(
                        serializers.Error_.parseOrThrow(_response.error.body, {
                            unrecognizedObjectKeys: "passthrough",
                            allowUnrecognizedUnionMembers: true,
                            allowUnrecognizedEnumValues: true,
                            skipValidation: true,
                            breadcrumbsPrefix: ["response"],
                        }),
                        _response.rawResponse,
                    );
                default:
                    throw new errors.WebflowError({
                        statusCode: _response.error.statusCode,
                        body: _response.error.body,
                        rawResponse: _response.rawResponse,
                    });
            }
        }

        return handleNonStatusCodeError(
            _response.error,
            _response.rawResponse,
            "GET",
            "/sites/{site_id}/analyze/reports/traffic",
        );
    }

    /**
     * Returns the most-visited pages over a time window, ranked by `sortBy` (sessions, users, or pageviews).
     *
     * Each row carries all three scope counts; `sortBy` only governs ordering. Filter the report with top-level query parameters (`country`, `deviceType`, `pagePath`, etc.) or via the `filter` parameter for multi-value and negation matching.
     *
     * Set `timeseries[bucketTimeZone]` to attach a daily pageview `timeseries` to each row. Bucket counts are always pageviews regardless of `sortBy` — row-level counts honor the requested sort; the timeseries does not.
     *
     * <Warning title="Analyze add-on required">This endpoint requires a workspace with the Analyze add-on.</Warning>
     *
     * <Note title="Concurrency limit: 1 request at a time">Each access token can have one Analyze request in flight at a time, across all Analyze endpoints. Additional concurrent requests return `429 Too Many Requests`; wait for your in-flight request to finish, or for the `Retry-After` interval, then retry.</Note>
     *
     * Required scope | `sites:read`
     *
     * @param {string} site_id - Unique identifier for a Site
     * @param {Webflow.analyze.ReportsTopPagesRequest} request
     * @param {ReportsClient.RequestOptions} requestOptions - Request-specific configuration.
     *
     * @throws {@link Webflow.BadRequestError}
     * @throws {@link Webflow.UnauthorizedError}
     * @throws {@link Webflow.ForbiddenError}
     * @throws {@link Webflow.NotFoundError}
     * @throws {@link Webflow.TooManyRequestsError}
     * @throws {@link Webflow.InternalServerError}
     *
     * @example
     *     await client.analyze.reports.topPages("580e63e98c9a982ac9b8b741", {
     *         startTime: new Date("2026-04-01T00:00:00.000Z"),
     *         endTime: new Date("2026-04-08T00:00:00.000Z"),
     *         sortBy: "session",
     *         limit: 1,
     *         deviceType: "desktop",
     *         country: "US",
     *         pagePath: "/towels",
     *         trafficSource: "SO",
     *         referrer: "google.com",
     *         browser: "Chrome",
     *         utmCampaign: "dont-panic-2026",
     *         utmMedium: "email",
     *         utmSource: "hitchhikers-guide"
     *     })
     */
    public topPages(
        site_id: string,
        request: Webflow.analyze.ReportsTopPagesRequest,
        requestOptions?: ReportsClient.RequestOptions,
    ): core.HttpResponsePromise<Webflow.TopPagesResponse> {
        return core.HttpResponsePromise.fromPromise(this.__topPages(site_id, request, requestOptions));
    }

    private async __topPages(
        site_id: string,
        request: Webflow.analyze.ReportsTopPagesRequest,
        requestOptions?: ReportsClient.RequestOptions,
    ): Promise<core.WithRawResponse<Webflow.TopPagesResponse>> {
        const {
            startTime,
            endTime,
            sortBy,
            limit,
            timeseries,
            deviceType,
            country,
            pagePath,
            trafficSource,
            referrer,
            browser,
            utmCampaign,
            utmMedium,
            utmSource,
            filter,
        } = request;
        const _queryParams: Record<string, unknown> = {
            startTime: startTime.toISOString(),
            endTime: endTime.toISOString(),
            sortBy:
                sortBy != null
                    ? serializers.TopPagesSortBy.jsonOrThrow(sortBy, {
                          unrecognizedObjectKeys: "passthrough",
                          allowUnrecognizedUnionMembers: true,
                          allowUnrecognizedEnumValues: true,
                          omitUndefined: true,
                      })
                    : undefined,
            limit,
            timeseries:
                timeseries != null
                    ? serializers.AnalyzeDailyTimeseriesQuery.jsonOrThrow(timeseries, {
                          unrecognizedObjectKeys: "passthrough",
                          allowUnrecognizedUnionMembers: true,
                          allowUnrecognizedEnumValues: true,
                          omitUndefined: true,
                          breadcrumbsPrefix: ["request", "timeseries"],
                      })
                    : timeseries,
            deviceType:
                deviceType != null
                    ? serializers.analyze.ReportsTopPagesRequestDeviceType.jsonOrThrow(deviceType, {
                          unrecognizedObjectKeys: "passthrough",
                          allowUnrecognizedUnionMembers: true,
                          allowUnrecognizedEnumValues: true,
                          omitUndefined: true,
                      })
                    : undefined,
            country,
            pagePath,
            trafficSource,
            referrer,
            browser,
            utmCampaign,
            utmMedium,
            utmSource,
            filter:
                filter != null
                    ? serializers.TopPagesFilter.jsonOrThrow(filter, {
                          unrecognizedObjectKeys: "passthrough",
                          allowUnrecognizedUnionMembers: true,
                          allowUnrecognizedEnumValues: true,
                          omitUndefined: true,
                          breadcrumbsPrefix: ["request", "filter"],
                      })
                    : filter,
        };
        const _authRequest: core.AuthRequest = await this._options.authProvider.getAuthRequest();
        const _headers: core.Fetcher.Args["headers"] = mergeHeaders(
            _authRequest.headers,
            this._options?.headers,
            requestOptions?.headers,
        );
        const _response = await core.fetcher({
            url: core.url.join(
                (await core.Supplier.get(this._options.baseUrl)) ??
                    ((await core.Supplier.get(this._options.environment)) ?? environments.WebflowEnvironment.DataApi)
                        .base,
                `sites/${core.url.encodePathParam(site_id)}/analyze/reports/top_pages`,
            ),
            method: "GET",
            headers: _headers,
            queryParameters: { ..._queryParams, ...requestOptions?.queryParams },
            timeoutMs: (requestOptions?.timeoutInSeconds ?? this._options?.timeoutInSeconds ?? 60) * 1000,
            maxRetries: requestOptions?.maxRetries ?? this._options?.maxRetries,
            abortSignal: requestOptions?.abortSignal,
            fetchFn: this._options?.fetch,
            logging: this._options.logging,
        });
        if (_response.ok) {
            return {
                data: serializers.TopPagesResponse.parseOrThrow(_response.body, {
                    unrecognizedObjectKeys: "passthrough",
                    allowUnrecognizedUnionMembers: true,
                    allowUnrecognizedEnumValues: true,
                    skipValidation: true,
                    breadcrumbsPrefix: ["response"],
                }),
                rawResponse: _response.rawResponse,
            };
        }

        if (_response.error.reason === "status-code") {
            switch (_response.error.statusCode) {
                case 400:
                    throw new Webflow.BadRequestError(_response.error.body, _response.rawResponse);
                case 401:
                    throw new Webflow.UnauthorizedError(
                        serializers.Error_.parseOrThrow(_response.error.body, {
                            unrecognizedObjectKeys: "passthrough",
                            allowUnrecognizedUnionMembers: true,
                            allowUnrecognizedEnumValues: true,
                            skipValidation: true,
                            breadcrumbsPrefix: ["response"],
                        }),
                        _response.rawResponse,
                    );
                case 403:
                    throw new Webflow.ForbiddenError(_response.error.body, _response.rawResponse);
                case 404:
                    throw new Webflow.NotFoundError(
                        serializers.Error_.parseOrThrow(_response.error.body, {
                            unrecognizedObjectKeys: "passthrough",
                            allowUnrecognizedUnionMembers: true,
                            allowUnrecognizedEnumValues: true,
                            skipValidation: true,
                            breadcrumbsPrefix: ["response"],
                        }),
                        _response.rawResponse,
                    );
                case 429:
                    throw new Webflow.TooManyRequestsError(
                        serializers.Error_.parseOrThrow(_response.error.body, {
                            unrecognizedObjectKeys: "passthrough",
                            allowUnrecognizedUnionMembers: true,
                            allowUnrecognizedEnumValues: true,
                            skipValidation: true,
                            breadcrumbsPrefix: ["response"],
                        }),
                        _response.rawResponse,
                    );
                case 500:
                    throw new Webflow.InternalServerError(
                        serializers.Error_.parseOrThrow(_response.error.body, {
                            unrecognizedObjectKeys: "passthrough",
                            allowUnrecognizedUnionMembers: true,
                            allowUnrecognizedEnumValues: true,
                            skipValidation: true,
                            breadcrumbsPrefix: ["response"],
                        }),
                        _response.rawResponse,
                    );
                default:
                    throw new errors.WebflowError({
                        statusCode: _response.error.statusCode,
                        body: _response.error.body,
                        rawResponse: _response.rawResponse,
                    });
            }
        }

        return handleNonStatusCodeError(
            _response.error,
            _response.rawResponse,
            "GET",
            "/sites/{site_id}/analyze/reports/top_pages",
        );
    }

    /**
     * Returns the top values within a chosen `dimension` — top countries, top traffic sources, top campaigns, top audiences, and so on — over a time window, ranked by sessions or users.
     *
     * Filter the report with top-level query parameters (`country`, `deviceType`, `pagePath`, etc.) or via the `filter` parameter for multi-value and negation matching.
     *
     * <Warning title="Analyze add-on required">This endpoint requires a workspace with the Analyze add-on.</Warning>
     *
     * <Note title="Concurrency limit: 1 request at a time">Each access token can have one Analyze request in flight at a time, across all Analyze endpoints. Additional concurrent requests return `429 Too Many Requests`; wait for your in-flight request to finish, or for the `Retry-After` interval, then retry.</Note>
     *
     * Required scope | `sites:read`
     *
     * @param {string} site_id - Unique identifier for a Site
     * @param {Webflow.analyze.ReportsTopDimensionsRequest} request
     * @param {ReportsClient.RequestOptions} requestOptions - Request-specific configuration.
     *
     * @throws {@link Webflow.BadRequestError}
     * @throws {@link Webflow.UnauthorizedError}
     * @throws {@link Webflow.ForbiddenError}
     * @throws {@link Webflow.NotFoundError}
     * @throws {@link Webflow.TooManyRequestsError}
     * @throws {@link Webflow.InternalServerError}
     *
     * @example
     *     await client.analyze.reports.topDimensions("580e63e98c9a982ac9b8b741", {
     *         startTime: new Date("2026-04-01T00:00:00.000Z"),
     *         endTime: new Date("2026-04-08T00:00:00.000Z"),
     *         dimension: "country",
     *         metricScope: "session",
     *         limit: 1,
     *         deviceType: "desktop",
     *         country: "US",
     *         pagePath: "/towels",
     *         trafficSource: "SO",
     *         referrer: "google.com",
     *         browser: "Chrome",
     *         utmCampaign: "dont-panic-2026",
     *         utmMedium: "email",
     *         utmSource: "hitchhikers-guide"
     *     })
     */
    public topDimensions(
        site_id: string,
        request: Webflow.analyze.ReportsTopDimensionsRequest,
        requestOptions?: ReportsClient.RequestOptions,
    ): core.HttpResponsePromise<Webflow.TopDimensionsResponse> {
        return core.HttpResponsePromise.fromPromise(this.__topDimensions(site_id, request, requestOptions));
    }

    private async __topDimensions(
        site_id: string,
        request: Webflow.analyze.ReportsTopDimensionsRequest,
        requestOptions?: ReportsClient.RequestOptions,
    ): Promise<core.WithRawResponse<Webflow.TopDimensionsResponse>> {
        const {
            startTime,
            endTime,
            dimension,
            metricScope,
            limit,
            deviceType,
            country,
            pagePath,
            trafficSource,
            referrer,
            browser,
            utmCampaign,
            utmMedium,
            utmSource,
            filter,
        } = request;
        const _queryParams: Record<string, unknown> = {
            startTime: startTime.toISOString(),
            endTime: endTime.toISOString(),
            dimension: serializers.TopDimensionsDimension.jsonOrThrow(dimension, {
                unrecognizedObjectKeys: "passthrough",
                allowUnrecognizedUnionMembers: true,
                allowUnrecognizedEnumValues: true,
                omitUndefined: true,
            }),
            metricScope: serializers.TopDimensionsMetricScope.jsonOrThrow(metricScope, {
                unrecognizedObjectKeys: "passthrough",
                allowUnrecognizedUnionMembers: true,
                allowUnrecognizedEnumValues: true,
                omitUndefined: true,
            }),
            limit,
            deviceType:
                deviceType != null
                    ? serializers.analyze.ReportsTopDimensionsRequestDeviceType.jsonOrThrow(deviceType, {
                          unrecognizedObjectKeys: "passthrough",
                          allowUnrecognizedUnionMembers: true,
                          allowUnrecognizedEnumValues: true,
                          omitUndefined: true,
                      })
                    : undefined,
            country,
            pagePath,
            trafficSource,
            referrer,
            browser,
            utmCampaign,
            utmMedium,
            utmSource,
            filter:
                filter != null
                    ? serializers.TopDimensionsFilter.jsonOrThrow(filter, {
                          unrecognizedObjectKeys: "passthrough",
                          allowUnrecognizedUnionMembers: true,
                          allowUnrecognizedEnumValues: true,
                          omitUndefined: true,
                          breadcrumbsPrefix: ["request", "filter"],
                      })
                    : filter,
        };
        const _authRequest: core.AuthRequest = await this._options.authProvider.getAuthRequest();
        const _headers: core.Fetcher.Args["headers"] = mergeHeaders(
            _authRequest.headers,
            this._options?.headers,
            requestOptions?.headers,
        );
        const _response = await core.fetcher({
            url: core.url.join(
                (await core.Supplier.get(this._options.baseUrl)) ??
                    ((await core.Supplier.get(this._options.environment)) ?? environments.WebflowEnvironment.DataApi)
                        .base,
                `sites/${core.url.encodePathParam(site_id)}/analyze/reports/top_dimensions`,
            ),
            method: "GET",
            headers: _headers,
            queryParameters: { ..._queryParams, ...requestOptions?.queryParams },
            timeoutMs: (requestOptions?.timeoutInSeconds ?? this._options?.timeoutInSeconds ?? 60) * 1000,
            maxRetries: requestOptions?.maxRetries ?? this._options?.maxRetries,
            abortSignal: requestOptions?.abortSignal,
            fetchFn: this._options?.fetch,
            logging: this._options.logging,
        });
        if (_response.ok) {
            return {
                data: serializers.TopDimensionsResponse.parseOrThrow(_response.body, {
                    unrecognizedObjectKeys: "passthrough",
                    allowUnrecognizedUnionMembers: true,
                    allowUnrecognizedEnumValues: true,
                    skipValidation: true,
                    breadcrumbsPrefix: ["response"],
                }),
                rawResponse: _response.rawResponse,
            };
        }

        if (_response.error.reason === "status-code") {
            switch (_response.error.statusCode) {
                case 400:
                    throw new Webflow.BadRequestError(_response.error.body, _response.rawResponse);
                case 401:
                    throw new Webflow.UnauthorizedError(
                        serializers.Error_.parseOrThrow(_response.error.body, {
                            unrecognizedObjectKeys: "passthrough",
                            allowUnrecognizedUnionMembers: true,
                            allowUnrecognizedEnumValues: true,
                            skipValidation: true,
                            breadcrumbsPrefix: ["response"],
                        }),
                        _response.rawResponse,
                    );
                case 403:
                    throw new Webflow.ForbiddenError(_response.error.body, _response.rawResponse);
                case 404:
                    throw new Webflow.NotFoundError(
                        serializers.Error_.parseOrThrow(_response.error.body, {
                            unrecognizedObjectKeys: "passthrough",
                            allowUnrecognizedUnionMembers: true,
                            allowUnrecognizedEnumValues: true,
                            skipValidation: true,
                            breadcrumbsPrefix: ["response"],
                        }),
                        _response.rawResponse,
                    );
                case 429:
                    throw new Webflow.TooManyRequestsError(
                        serializers.Error_.parseOrThrow(_response.error.body, {
                            unrecognizedObjectKeys: "passthrough",
                            allowUnrecognizedUnionMembers: true,
                            allowUnrecognizedEnumValues: true,
                            skipValidation: true,
                            breadcrumbsPrefix: ["response"],
                        }),
                        _response.rawResponse,
                    );
                case 500:
                    throw new Webflow.InternalServerError(
                        serializers.Error_.parseOrThrow(_response.error.body, {
                            unrecognizedObjectKeys: "passthrough",
                            allowUnrecognizedUnionMembers: true,
                            allowUnrecognizedEnumValues: true,
                            skipValidation: true,
                            breadcrumbsPrefix: ["response"],
                        }),
                        _response.rawResponse,
                    );
                default:
                    throw new errors.WebflowError({
                        statusCode: _response.error.statusCode,
                        body: _response.error.body,
                        rawResponse: _response.rawResponse,
                    });
            }
        }

        return handleNonStatusCodeError(
            _response.error,
            _response.rawResponse,
            "GET",
            "/sites/{site_id}/analyze/reports/top_dimensions",
        );
    }

    /**
     * Returns the top events over a time window, ranked by how often they occurred.
     *
     * Events are counted individually, not rolled up into sessions, users, or pageviews — so this report has no `metricScope`. Each row's `count` is how many times the event occurred. Filter the report with top-level query parameters (`country`, `deviceType`, `pagePath`, etc.) or via the `filter` parameter for multi-value and negation matching.
     *
     * Set `timeseries[bucketTimeZone]` to attach a daily event count `timeseries` to each row.
     *
     * <Warning title="Analyze add-on required">This endpoint requires a workspace with the Analyze add-on.</Warning>
     *
     * <Note title="Concurrency limit: 1 request at a time">Each access token can have one Analyze request in flight at a time, across all Analyze endpoints. Additional concurrent requests return `429 Too Many Requests`; wait for your in-flight request to finish, or for the `Retry-After` interval, then retry.</Note>
     *
     * Required scope | `sites:read`
     *
     * @param {string} site_id - Unique identifier for a Site
     * @param {Webflow.analyze.ReportsTopEventsRequest} request
     * @param {ReportsClient.RequestOptions} requestOptions - Request-specific configuration.
     *
     * @throws {@link Webflow.BadRequestError}
     * @throws {@link Webflow.UnauthorizedError}
     * @throws {@link Webflow.ForbiddenError}
     * @throws {@link Webflow.NotFoundError}
     * @throws {@link Webflow.TooManyRequestsError}
     * @throws {@link Webflow.InternalServerError}
     *
     * @example
     *     await client.analyze.reports.topEvents("580e63e98c9a982ac9b8b741", {
     *         startTime: new Date("2026-04-01T00:00:00.000Z"),
     *         endTime: new Date("2026-04-08T00:00:00.000Z"),
     *         limit: 1,
     *         deviceType: "desktop",
     *         country: "US",
     *         pagePath: "/towels",
     *         trafficSource: "SO",
     *         browser: "Chrome",
     *         utmCampaign: "dont-panic-2026",
     *         utmMedium: "email",
     *         utmSource: "hitchhikers-guide"
     *     })
     */
    public topEvents(
        site_id: string,
        request: Webflow.analyze.ReportsTopEventsRequest,
        requestOptions?: ReportsClient.RequestOptions,
    ): core.HttpResponsePromise<Webflow.TopEventsResponse> {
        return core.HttpResponsePromise.fromPromise(this.__topEvents(site_id, request, requestOptions));
    }

    private async __topEvents(
        site_id: string,
        request: Webflow.analyze.ReportsTopEventsRequest,
        requestOptions?: ReportsClient.RequestOptions,
    ): Promise<core.WithRawResponse<Webflow.TopEventsResponse>> {
        const {
            startTime,
            endTime,
            limit,
            timeseries,
            deviceType,
            country,
            pagePath,
            trafficSource,
            browser,
            utmCampaign,
            utmMedium,
            utmSource,
            filter,
        } = request;
        const _queryParams: Record<string, unknown> = {
            startTime: startTime.toISOString(),
            endTime: endTime.toISOString(),
            limit,
            timeseries:
                timeseries != null
                    ? serializers.AnalyzeDailyTimeseriesQuery.jsonOrThrow(timeseries, {
                          unrecognizedObjectKeys: "passthrough",
                          allowUnrecognizedUnionMembers: true,
                          allowUnrecognizedEnumValues: true,
                          omitUndefined: true,
                          breadcrumbsPrefix: ["request", "timeseries"],
                      })
                    : timeseries,
            deviceType:
                deviceType != null
                    ? serializers.analyze.ReportsTopEventsRequestDeviceType.jsonOrThrow(deviceType, {
                          unrecognizedObjectKeys: "passthrough",
                          allowUnrecognizedUnionMembers: true,
                          allowUnrecognizedEnumValues: true,
                          omitUndefined: true,
                      })
                    : undefined,
            country,
            pagePath,
            trafficSource,
            browser,
            utmCampaign,
            utmMedium,
            utmSource,
            filter:
                filter != null
                    ? serializers.TopEventsFilter.jsonOrThrow(filter, {
                          unrecognizedObjectKeys: "passthrough",
                          allowUnrecognizedUnionMembers: true,
                          allowUnrecognizedEnumValues: true,
                          omitUndefined: true,
                          breadcrumbsPrefix: ["request", "filter"],
                      })
                    : filter,
        };
        const _authRequest: core.AuthRequest = await this._options.authProvider.getAuthRequest();
        const _headers: core.Fetcher.Args["headers"] = mergeHeaders(
            _authRequest.headers,
            this._options?.headers,
            requestOptions?.headers,
        );
        const _response = await core.fetcher({
            url: core.url.join(
                (await core.Supplier.get(this._options.baseUrl)) ??
                    ((await core.Supplier.get(this._options.environment)) ?? environments.WebflowEnvironment.DataApi)
                        .base,
                `sites/${core.url.encodePathParam(site_id)}/analyze/reports/top_events`,
            ),
            method: "GET",
            headers: _headers,
            queryParameters: { ..._queryParams, ...requestOptions?.queryParams },
            timeoutMs: (requestOptions?.timeoutInSeconds ?? this._options?.timeoutInSeconds ?? 60) * 1000,
            maxRetries: requestOptions?.maxRetries ?? this._options?.maxRetries,
            abortSignal: requestOptions?.abortSignal,
            fetchFn: this._options?.fetch,
            logging: this._options.logging,
        });
        if (_response.ok) {
            return {
                data: serializers.TopEventsResponse.parseOrThrow(_response.body, {
                    unrecognizedObjectKeys: "passthrough",
                    allowUnrecognizedUnionMembers: true,
                    allowUnrecognizedEnumValues: true,
                    skipValidation: true,
                    breadcrumbsPrefix: ["response"],
                }),
                rawResponse: _response.rawResponse,
            };
        }

        if (_response.error.reason === "status-code") {
            switch (_response.error.statusCode) {
                case 400:
                    throw new Webflow.BadRequestError(_response.error.body, _response.rawResponse);
                case 401:
                    throw new Webflow.UnauthorizedError(
                        serializers.Error_.parseOrThrow(_response.error.body, {
                            unrecognizedObjectKeys: "passthrough",
                            allowUnrecognizedUnionMembers: true,
                            allowUnrecognizedEnumValues: true,
                            skipValidation: true,
                            breadcrumbsPrefix: ["response"],
                        }),
                        _response.rawResponse,
                    );
                case 403:
                    throw new Webflow.ForbiddenError(_response.error.body, _response.rawResponse);
                case 404:
                    throw new Webflow.NotFoundError(
                        serializers.Error_.parseOrThrow(_response.error.body, {
                            unrecognizedObjectKeys: "passthrough",
                            allowUnrecognizedUnionMembers: true,
                            allowUnrecognizedEnumValues: true,
                            skipValidation: true,
                            breadcrumbsPrefix: ["response"],
                        }),
                        _response.rawResponse,
                    );
                case 429:
                    throw new Webflow.TooManyRequestsError(
                        serializers.Error_.parseOrThrow(_response.error.body, {
                            unrecognizedObjectKeys: "passthrough",
                            allowUnrecognizedUnionMembers: true,
                            allowUnrecognizedEnumValues: true,
                            skipValidation: true,
                            breadcrumbsPrefix: ["response"],
                        }),
                        _response.rawResponse,
                    );
                case 500:
                    throw new Webflow.InternalServerError(
                        serializers.Error_.parseOrThrow(_response.error.body, {
                            unrecognizedObjectKeys: "passthrough",
                            allowUnrecognizedUnionMembers: true,
                            allowUnrecognizedEnumValues: true,
                            skipValidation: true,
                            breadcrumbsPrefix: ["response"],
                        }),
                        _response.rawResponse,
                    );
                default:
                    throw new errors.WebflowError({
                        statusCode: _response.error.statusCode,
                        body: _response.error.body,
                        rawResponse: _response.rawResponse,
                    });
            }
        }

        return handleNonStatusCodeError(
            _response.error,
            _response.rawResponse,
            "GET",
            "/sites/{site_id}/analyze/reports/top_events",
        );
    }

    /**
     * Returns the average time on page over a time window — as a single aggregate value, or bucketed by day or week when `timeseries` is supplied.
     *
     * Choose how the average is computed with `metricScope` (per session, user, or pageview). Filter the report with top-level query parameters (`country`, `deviceType`, `pagePath`, etc.) or via the `filter` parameter for multi-value and negation matching.
     *
     * <Warning title="Analyze add-on required">This endpoint requires a workspace with the Analyze add-on.</Warning>
     *
     * <Note title="Concurrency limit: 1 request at a time">Each access token can have one Analyze request in flight at a time, across all Analyze endpoints. Additional concurrent requests return `429 Too Many Requests`; wait for your in-flight request to finish, or for the `Retry-After` interval, then retry.</Note>
     *
     * Required scope | `sites:read`
     *
     * @param {string} site_id - Unique identifier for a Site
     * @param {Webflow.analyze.ReportsTimeOnPageRequest} request
     * @param {ReportsClient.RequestOptions} requestOptions - Request-specific configuration.
     *
     * @throws {@link Webflow.BadRequestError}
     * @throws {@link Webflow.UnauthorizedError}
     * @throws {@link Webflow.ForbiddenError}
     * @throws {@link Webflow.NotFoundError}
     * @throws {@link Webflow.TooManyRequestsError}
     * @throws {@link Webflow.InternalServerError}
     *
     * @example
     *     await client.analyze.reports.timeOnPage("580e63e98c9a982ac9b8b741", {
     *         startTime: new Date("2026-04-01T00:00:00.000Z"),
     *         endTime: new Date("2026-04-08T00:00:00.000Z"),
     *         metricScope: "session",
     *         deviceType: "desktop",
     *         country: "US",
     *         pagePath: "/towels",
     *         trafficSource: "SO",
     *         referrer: "google.com",
     *         browser: "Chrome",
     *         utmCampaign: "dont-panic-2026",
     *         utmMedium: "email",
     *         utmSource: "hitchhikers-guide"
     *     })
     */
    public timeOnPage(
        site_id: string,
        request: Webflow.analyze.ReportsTimeOnPageRequest,
        requestOptions?: ReportsClient.RequestOptions,
    ): core.HttpResponsePromise<Webflow.TimeOnPageResponse> {
        return core.HttpResponsePromise.fromPromise(this.__timeOnPage(site_id, request, requestOptions));
    }

    private async __timeOnPage(
        site_id: string,
        request: Webflow.analyze.ReportsTimeOnPageRequest,
        requestOptions?: ReportsClient.RequestOptions,
    ): Promise<core.WithRawResponse<Webflow.TimeOnPageResponse>> {
        const {
            startTime,
            endTime,
            metricScope,
            timeseries,
            deviceType,
            country,
            pagePath,
            trafficSource,
            referrer,
            browser,
            utmCampaign,
            utmMedium,
            utmSource,
            filter,
        } = request;
        const _queryParams: Record<string, unknown> = {
            startTime: startTime.toISOString(),
            endTime: endTime.toISOString(),
            metricScope: serializers.TimeOnPageMetricScope.jsonOrThrow(metricScope, {
                unrecognizedObjectKeys: "passthrough",
                allowUnrecognizedUnionMembers: true,
                allowUnrecognizedEnumValues: true,
                omitUndefined: true,
            }),
            timeseries:
                timeseries != null
                    ? serializers.TimeOnPageTimeseriesQuery.jsonOrThrow(timeseries, {
                          unrecognizedObjectKeys: "passthrough",
                          allowUnrecognizedUnionMembers: true,
                          allowUnrecognizedEnumValues: true,
                          omitUndefined: true,
                          breadcrumbsPrefix: ["request", "timeseries"],
                      })
                    : timeseries,
            deviceType:
                deviceType != null
                    ? serializers.analyze.ReportsTimeOnPageRequestDeviceType.jsonOrThrow(deviceType, {
                          unrecognizedObjectKeys: "passthrough",
                          allowUnrecognizedUnionMembers: true,
                          allowUnrecognizedEnumValues: true,
                          omitUndefined: true,
                      })
                    : undefined,
            country,
            pagePath,
            trafficSource,
            referrer,
            browser,
            utmCampaign,
            utmMedium,
            utmSource,
            filter:
                filter != null
                    ? serializers.TimeOnPageFilter.jsonOrThrow(filter, {
                          unrecognizedObjectKeys: "passthrough",
                          allowUnrecognizedUnionMembers: true,
                          allowUnrecognizedEnumValues: true,
                          omitUndefined: true,
                          breadcrumbsPrefix: ["request", "filter"],
                      })
                    : filter,
        };
        const _authRequest: core.AuthRequest = await this._options.authProvider.getAuthRequest();
        const _headers: core.Fetcher.Args["headers"] = mergeHeaders(
            _authRequest.headers,
            this._options?.headers,
            requestOptions?.headers,
        );
        const _response = await core.fetcher({
            url: core.url.join(
                (await core.Supplier.get(this._options.baseUrl)) ??
                    ((await core.Supplier.get(this._options.environment)) ?? environments.WebflowEnvironment.DataApi)
                        .base,
                `sites/${core.url.encodePathParam(site_id)}/analyze/reports/time_on_page`,
            ),
            method: "GET",
            headers: _headers,
            queryParameters: { ..._queryParams, ...requestOptions?.queryParams },
            timeoutMs: (requestOptions?.timeoutInSeconds ?? this._options?.timeoutInSeconds ?? 60) * 1000,
            maxRetries: requestOptions?.maxRetries ?? this._options?.maxRetries,
            abortSignal: requestOptions?.abortSignal,
            fetchFn: this._options?.fetch,
            logging: this._options.logging,
        });
        if (_response.ok) {
            return {
                data: serializers.TimeOnPageResponse.parseOrThrow(_response.body, {
                    unrecognizedObjectKeys: "passthrough",
                    allowUnrecognizedUnionMembers: true,
                    allowUnrecognizedEnumValues: true,
                    skipValidation: true,
                    breadcrumbsPrefix: ["response"],
                }),
                rawResponse: _response.rawResponse,
            };
        }

        if (_response.error.reason === "status-code") {
            switch (_response.error.statusCode) {
                case 400:
                    throw new Webflow.BadRequestError(_response.error.body, _response.rawResponse);
                case 401:
                    throw new Webflow.UnauthorizedError(
                        serializers.Error_.parseOrThrow(_response.error.body, {
                            unrecognizedObjectKeys: "passthrough",
                            allowUnrecognizedUnionMembers: true,
                            allowUnrecognizedEnumValues: true,
                            skipValidation: true,
                            breadcrumbsPrefix: ["response"],
                        }),
                        _response.rawResponse,
                    );
                case 403:
                    throw new Webflow.ForbiddenError(_response.error.body, _response.rawResponse);
                case 404:
                    throw new Webflow.NotFoundError(
                        serializers.Error_.parseOrThrow(_response.error.body, {
                            unrecognizedObjectKeys: "passthrough",
                            allowUnrecognizedUnionMembers: true,
                            allowUnrecognizedEnumValues: true,
                            skipValidation: true,
                            breadcrumbsPrefix: ["response"],
                        }),
                        _response.rawResponse,
                    );
                case 429:
                    throw new Webflow.TooManyRequestsError(
                        serializers.Error_.parseOrThrow(_response.error.body, {
                            unrecognizedObjectKeys: "passthrough",
                            allowUnrecognizedUnionMembers: true,
                            allowUnrecognizedEnumValues: true,
                            skipValidation: true,
                            breadcrumbsPrefix: ["response"],
                        }),
                        _response.rawResponse,
                    );
                case 500:
                    throw new Webflow.InternalServerError(
                        serializers.Error_.parseOrThrow(_response.error.body, {
                            unrecognizedObjectKeys: "passthrough",
                            allowUnrecognizedUnionMembers: true,
                            allowUnrecognizedEnumValues: true,
                            skipValidation: true,
                            breadcrumbsPrefix: ["response"],
                        }),
                        _response.rawResponse,
                    );
                default:
                    throw new errors.WebflowError({
                        statusCode: _response.error.statusCode,
                        body: _response.error.body,
                        rawResponse: _response.rawResponse,
                    });
            }
        }

        return handleNonStatusCodeError(
            _response.error,
            _response.rawResponse,
            "GET",
            "/sites/{site_id}/analyze/reports/time_on_page",
        );
    }
}
