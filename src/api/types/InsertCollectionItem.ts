// This file was auto-generated by Fern from our API Definition.

import type * as Webflow from "../index";

/**
 * A single entry in an `items/insert` request. Each entry describes one item and the locales to create it in.
 *
 * - Omit `id` to create a new item. Use `cmsLocaleIds` to select locales, or `allCmsLocales: true` to select every current site locale. The variants share one new item ID and the same `fieldData`. Omit both locale selectors to create only in the primary locale.
 * - Set `id` to an existing item's ID to add locale variants to that item. `cmsLocaleIds` is required when you send `id`, and the item must not already have a variant in those locales.
 * - To give one item different content per locale, send several entries that share the same `id`, each with a single locale in `cmsLocaleIds`.
 */
export interface InsertCollectionItem {
    /** The ID of an existing item to attach locale variants to. Omit this property to create a new item. Requires `cmsLocaleIds` and cannot be combined with `allCmsLocales`. */
    id?: string;
    /** The locales to create this entry in. For a new item, omit both this property and `allCmsLocales` to create only in the primary locale. Required when `id` is set. Cannot be combined with `allCmsLocales`, even as an empty array. A locale listed twice returns a `400`. */
    cmsLocaleIds?: string[];
    /** Set to `true` to create a new item in every current site locale, including the primary locale, without listing locale IDs. The variants share one new item ID and the same `fieldData`. Only `true` is accepted; omit this property instead of sending `false`. Cannot be combined with `cmsLocaleIds` or an existing item `id`. Each locale variant counts toward the 100-item request limit. */
    allCmsLocales?: true;
    /** Boolean determining if the Item is set to archived */
    isArchived?: boolean;
    /** Whether the item is created in a draft state. A new item has never been published, so `isDraft: true` gives it a `Draft` status. Set `isDraft: false` to queue the item to publish on the next site publish. */
    isDraft?: boolean;
    fieldData: Webflow.InsertCollectionItemFieldData;
}
