// This file was auto-generated by Fern from our API Definition.

/**
 * A single **daily** bucket in a per-event timeseries. Returned only when `timeseries` is requested.
 *
 * Buckets are aligned to local day boundaries in the response's `bucketing.bucketTimeZone`; the `timestamp` is the UTC instant for that local bucket start. A row's `timeseries` contains daily buckets for the requested window. When `timeseries` is requested but the event did not occur in the window, `timeseries` is an empty array — distinguishing "asked, none" from "didn't ask".
 */
export interface TopEventsTimeseriesPoint {
    /** Start of the daily bucket, in ISO 8601 / RFC 3339 format. */
    timestamp: Date;
    /** Number of times the event occurred on this day. */
    count: number;
}
