// This file was auto-generated by Fern from our API Definition.

/**
 * A single **daily** bucket in a per-page timeseries. Returned only when `timeseries` is requested.
 *
 * Buckets are aligned to local day boundaries in the response's `bucketing.bucketTimeZone`; the `timestamp` is the UTC instant for that local bucket start. A row's `timeseries` contains daily buckets for the requested window.
 *
 * Bucket counts are always pageviews, regardless of the request's `sortBy`. Row-level `sessionCount` / `userCount` / `pageviewCount` reflect `sortBy`.
 */
export interface TopPagesTimeseriesPoint {
    /** Start of the daily bucket, in ISO 8601 / RFC 3339 format. */
    timestamp: Date;
    /** Pageviews recorded for this day. */
    pageviewCount: number;
}
