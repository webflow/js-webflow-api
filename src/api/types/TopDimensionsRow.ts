// This file was auto-generated by Fern from our API Definition.

/**
 * A single value within the chosen dimension, with its count for the requested `metricScope`.
 */
export interface TopDimensionsRow {
    /** Identifier for the dimension value. Pass this back into a `filter` on any Analyze report to scope that report to this value. */
    attributeKey: string;
    /** Display label for the dimension value. For many dimensions this matches `attributeKey`; for others (such as `region`, `trafficSource`, and `audienceIds`) it's a human-readable label. */
    name: string;
    /** Non-negative count for this dimension value, in units of the request's `metricScope`. */
    count: number;
}
